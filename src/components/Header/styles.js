/**
 * Module dependencies
 */
import React from 'react';
import {
  StyleSheet,
} from 'react-native';

/**
 * Styles
 */
const styles = StyleSheet.create({
  header: {
    minWidth: '100%',
    position: 'absolute',
  },
});

/**
 * Expose
 */
export default styles;
